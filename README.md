# go-temporal-pg
Demo Temporal for Postgres Orchestration

## Development

The repository uses [mise](https://mise.jdx.dev/) to pin Go 1.27.0 and run
tasks across both Go modules.

```sh
mise install
mise run fix
mise run tidy
mise run check
```
